import static java.lang.System.in;
import static java.lang.System.out;
import java.util.Scanner;

class Math1 {
    static int sum(int a, int b) {
        return a + b;
    }

    static int des (int a, int b) {
        return a - b;
    }

    static int chasnoe (int a, int b) {
        return a * b;
    }

    static double del (double a, double b) {
        return a / b;
    }
}

public class feb27LessonJava_1 {
    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
      /*  int z = sc.nextInt();
        int x = sc.nextInt();
        int y = sc.nextInt();
        int result;
        result = Math.sum(x, y);
        out.println(result);
        result = Math.des(x, y);
        out.println(result);
        result = Math.chasnoe(x, y);
        out.println(result);
        double a1 = sc.nextDouble();
        double b1 = sc.nextDouble();
        double result1 = Math.del(a1, b1);
        out.println(result1);
        int sec;
        int minut = 60;
        int hour = 60 * 60;
        //4:51:21;
        int morning = 4*(hour)+51*(minut)+ 21;
        out.println(morning);
                        //Дано количество секунд с начала суток. Вычислить текущее время
        int sec = 5435;
        int hour;
        hour = sec / 3600;
        out.println(hour);
        int min = (sec%3600)/60;
        out.println(min);
        int secNow = ((sec%3600)%60);
        out.println(secNow);

                    //Дана длина ребра куба. Вычислить площадь его боковой поверхности
        double s, h = 2.4;
        s = 6 * (Math.pow(h, 2));
        out.println(s);

                    //6.	Даны длины катетов прямоугольного треугольника. Вычислить его периметр.
        int perimetr;
        int kat = 8;
        perimetr = kat*3;
        out.println(perimetr);

                    //7.	Дана длина окружности. Найти площадь данного круга.

        double S, R, L = 23.3;
        double pi = 3.14;
        R = L/(2*pi);
        S = pi*(Math.pow(R, 2));
        out.println(S);

                    //8.	Даны координаты двух точек. Вычислить расстояние между ними.
        int a = 32, b = 34;
        int x = 43, y = 3;//Sqrt(Sqr(x1-x2) + Sqr(y1-y2));
        double L = (Math.sqrt((Math.pow(a-x, 2)) + (Math.pow(b-y, 2))));
        out.println(L);

                    //9.	Даны два числа. Вычислить среднее геометрическое их модулей
        double srGeo;
        int x = 4, y = 3;
        srGeo = Math.sqrt(Math.abs(x) + Math.abs(y));
        out.println(srGeo);
                    //10.	Дано целое число. Определить в нем количество сотен (не включающее количество тысяч).
                        */

    Переход на 11



    }
}
